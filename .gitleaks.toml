title = "gitleaks config: Haushaltsauktion"

[extend]
useDefault = true

[allowlist]
description = "Generated build artifacts and lockfile integrity hashes — not secrets"
paths = [
  '''package-lock\.json''',
  '''apps/web/dist/.*''',
  '''.*\.tsbuildinfo''',
  '''\.planning/cache/.*''',
]
